Three things are required when installing dotfiles:

installing packages
defining configs
running commands

The third step can include installations outside of package managers/brew etc, performing setup steps or setting env variables.
